<div class="container">
  <div class="row">
    <div class="col-sm-3">
      <h2 class="head-line title_h2 text-center bg-light">来店予約一覧</h2>
    </div>
  </div>
</div>
<br>
<div class="container">
  <div class="row">
    <table class="table table-hover table-condensed">
      <thead>
        <tr class="info">
          <th class="bg-light" style="width: 5%">予約ID</th>
          <th class="bg-light" style="width: 25%">氏名</th>
          <th class="bg-light" style="width: 10%">来店時間</th>
          <th class="bg-light" style="width: 40%">店舗名</th>
          <th class="bg-light" style="width: 15%">ステータス</th>
        </tr>
      </thead>
      <tbody>
        <% @visits.each do |visit| %>
          <tr>
            <td class="info", style="width: 10%">
              <%= visit.id %>
            </td>
            <td class="text-left", style="width: 15%">
              <%= visit.name %>(<%= visit.name_kana %>)
            </td>
            <td class="text-left", style="width: 15%">
              <%= link_to admin_visit_path(visit), class: "btn-link" do %>
                <u class="text-dark"><%= visit.visit %></u>
              <% end %>
            </td>
            <td class="text-left", style="width: 15%">
              <%= visit.dealer.address %>
            </td>
            <td class="text-left", style="width: 15%">
              <% if visit.visit_status == '来店待ち' %>
                <p class="text-success mt-2">来店待ち</p>
              <% elsif visit.visit_status == '接客中' %>
                <p class="text-danger mt-2">接客中</p>
              <% elsif visit.visit_status == '接客済み' %>
                <p class="text-secondary mt-2">接客済み</p>
              <% else %>
                <p class="text-primary mt-2">キャンセル</p>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <%= paginate @visits, class: "text-center" %>
</div>